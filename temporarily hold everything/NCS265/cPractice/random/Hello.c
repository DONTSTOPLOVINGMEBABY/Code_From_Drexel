#include <stdio.h>

int main() { 
	int i = 13 ;
	double d = 1.618034 ;
	char name[20] = "The Lorax" ; 

		// printf prints to stdout

	printf( "I am %s, age %d, at %f\n", name, i, d ) ;

	return (0) ; 
}
