#include <stdio.h> 

int main(int argc, char *argv[] ) 
{
	int i ;

	for( i = 0; i<argc; ++i) 
		printf( "%3d  %s\n", i, argv[i] ) ; 



	printf("lets go mothafuckas. squad out. roll up. it's peacetime, biatch. let's ride. let's go. let's get it. let's run it. let's swank it. let's fucking wreck it. okaay. alright. nice. uh-oh that's right, nice. will talk to you soon; get back to ya later. alright. and then we gon keep talking about this long string that i cannot stop typing. oh my goodnes. do you think it will still compile? I reaally hope so. ") ; 


	return( 0 ) ; 
	 
}
